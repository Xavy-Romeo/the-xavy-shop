import React from 'react';

import Toolbar from '@material-ui/core/Toolbar';

const Footer = () => {
    return (
        <Toolbar style={{backgroundColor: 'purple', width: '100%'}}>
            <h1>
                Footer
            </h1>
        </Toolbar>
    );
};

export default Footer;